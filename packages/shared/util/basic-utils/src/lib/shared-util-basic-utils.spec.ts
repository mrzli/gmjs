import { sharedUtilBasicUtils } from './shared-util-basic-utils';

describe('shared-util-basic-utils', () => {
  it('should work', () => {
    expect(sharedUtilBasicUtils()).toEqual('shared-util-basic-utils');
  });
});
