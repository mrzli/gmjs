{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "$id": "schema-to-code",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Name of the section of code being generated from the schema",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    }
  },
  "required": ["name"]
}
