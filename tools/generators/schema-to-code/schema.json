{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "$id": "schema-to-code",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Generation code section name.",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    }
  },
  "required": ["name"]
}
